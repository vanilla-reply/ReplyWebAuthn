{% sw_extends '@Storefront/storefront/page/account/sidebar.html.twig' %}

{% block page_account_sidebar_menu_inner %}
    {{ parent() }}

    {% block page_account_sidebar_link_credentials %}
        <a href="{{ path('frontend.account.credential.page') }}"
           title="{{ "reply-web-authn.account.credentialLink"|trans }}"
           class="list-group-item list-group-item-action account-aside-item{% if controllerName is same as('AccountCredential') and controllerAction is same as('overviewPage') %} is-active{% endif %}">
            {{ "reply-web-authn.account.credentialLink"|trans }}
        </a>
    {% endblock %}
{% endblock %}
